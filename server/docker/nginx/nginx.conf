worker_processes auto;

events {
    worker_connections 1024;
}

http {
    include       mime.types;
    default_type  application/octet-stream;
    sendfile        on;
    keepalive_timeout  65;
    client_max_body_size 64M;

    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_prefer_server_ciphers on;
    ssl_session_cache shared:SSL:10m;

    upstream app {
        server app:9000;
    }

    # 80 -> 443 redirect (localhost)
    server {
        listen 80;
        server_name localhost;
        return 301 https://$host$request_uri;
    }

    # localhost HTTPS
    server {
        listen 443 ssl;
        server_name localhost;
        root /var/www/html/_webroot;

        ssl_certificate     /etc/nginx/certs/localhost+3.pem;
        ssl_certificate_key /etc/nginx/certs/localhost+3-key.pem;

        index index.php index.html index.htm;

        location / {
            try_files $uri $uri/ /index.php?$query_string;
        }

        location ~ \.php$ {
            include fastcgi_params;
            fastcgi_pass app;
            fastcgi_index index.php;
            fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        }

        location ~ /\.ht {
            deny all;
        }
    }

    # 80 -> 443 redirect (cms.localhost)
    server {
        listen 80;
        server_name cms.localhost;
        return 301 https://$host$request_uri;
    }

    # cms.localhost HTTPS
    server {
        listen 443 ssl;
        server_name cms.localhost;
        root /var/www/html/_cms-webroot;

        ssl_certificate     /etc/nginx/certs/localhost+3.pem;
        ssl_certificate_key /etc/nginx/certs/localhost+3-key.pem;

        index index.php index.html index.htm;

        location / {
            try_files $uri $uri/ /index.php?$query_string;
        }

        location ~ \.php$ {
            include fastcgi_params;
            fastcgi_pass app;
            fastcgi_index index.php;
            fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        }

        location ~ /\.ht {
            deny all;
        }
    }

    # 80 -> 443 redirect (testing.localhost)
    server {
        listen 80;
        server_name testing.localhost;
        return 301 https://$host$request_uri;
    }

    # testing.localhost HTTPS
    server {
        listen 443 ssl;
        server_name testing.localhost;
        root /var/www/html/_testing-webroot;

        ssl_certificate     /etc/nginx/certs/localhost+3.pem;
        ssl_certificate_key /etc/nginx/certs/localhost+3-key.pem;

        index index.php index.html index.htm;

        location / {
            try_files $uri $uri/ /index.php?$query_string;
        }

        location ~ \.php$ {
            include fastcgi_params;
            fastcgi_pass app;
            fastcgi_index index.php;
            fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        }

        location ~ /\.ht {
            deny all;
        }
    }

    # 80 -> 443 redirect (api.localhost)
    server {
        listen 80;
        server_name api.localhost;
        return 301 https://$host$request_uri;
    }

    # api.localhost HTTPS
    server {
        listen 443 ssl;
        server_name api.localhost;
        root /var/www/html/_webroot;

        ssl_certificate     /etc/nginx/certs/localhost+3.pem;
        ssl_certificate_key /etc/nginx/certs/localhost+3-key.pem;

        index index.php index.html index.htm;

        location / {
            try_files $uri $uri/ /index.php?$query_string;
        }

        location ~ \.php$ {
            include fastcgi_params;
            fastcgi_pass app;
            fastcgi_index index.php;
            fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        }

        location ~ /\.ht {
            deny all;
        }
    }
} 
