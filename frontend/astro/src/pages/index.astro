---
import Layout from '@/layouts/Layout.astro';
import siteData from '../../siteData.mjs';

const pageId = 'home',
  pageMeta = siteData[pageId],
  ldJSON = [
    {
      '@type': 'ListItem',
      position: 1,
      item: {
        '@id': `${import.meta.env.PUBLIC_ENVURL}`,
        name: 'HOME'
      }
    }
  ];
---

<Layout
  pageName={pageId}
  title={pageMeta.title}
  description={pageMeta.description}
  ogpImage={pageMeta.ogpImage}
  type={pageMeta.type}
  path={pageMeta.path}
  ldJSON={ldJSON}
>
  <main class="l-main p-front-page p-page">
    <article>
      <section class="l-wrapper">
        <div class="l-container">
          <div class="l-inner">
            <div class="__block-1">
              <h2>トップページ</h2>
              <div id="app-react"></div>
              <div id="app-vue"></div>
              <div id="app-three" style="width: 600px; height: 300px; max-width: 100%;"></div>
            </div>
          </div>
        </div>
      </section>
    </article>
  </main>
</Layout>

<style lang="scss" is:global></style>
